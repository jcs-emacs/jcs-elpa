
Show SLY result with sideline.

1) Add sideline-sly to sideline backends list,

  (setq sideline-backends-right '(sideline-sly))

2) Then enable sideline-mode in the target buffer,

  M-x sideline-mode
